# Ops Agent 面试准备（照着背 + 照着讲）

## 一、30 秒项目介绍（开场必说）

> "我做了一个 AI 智能运维助手，解决的问题是：运维新人面对'网站打不开'这类故障时不知道从哪查起。我用 Python + FastAPI 做后端，接 DeepSeek 的 LLM，通过 Tool Calling 让大模型能自己决定调用 DNS 解析、ping、端口检测、HTTP 检查这些真实工具，按'DNS → 连通性 → 端口 → HTTP 服务'的标准排查链路逐层检测，最后基于**真实检测结果**给出排查结论，而不是像普通聊天机器人那样凭空给一堆建议。前端是一个聊天页面，能看到 Agent 每一步调用了什么工具。"

## 二、三个必问问题及答法

### Q1：Agent 和普通调 LLM 有什么区别？（最核心）

> "普通 LLM 应用是'一问一答'，模型只能说不能做。我的 Agent 是一个**循环**：LLM 先思考，如果需要真实数据就返回一个 tool_call（比如调 dns_lookup），我的代码执行这个工具、把结果追加到对话历史里再喂回 LLM，LLM 看到真实结果后决定继续查还是给结论。这个循环最多跑 6 轮防止死循环。核心区别就是 Agent 能**根据中间结果决定下一步做什么**。"

追问"怎么实现的？是不是用了 LangChain？"——
> "没用框架，我手写的循环，几十行代码。先定义工具的 JSON Schema 给模型看，模型返回 tool_calls，我按名字映射到 Python 函数执行。手写让我真正理解了 Tool Calling 的机制，之后再看 LangChain/LangGraph 就是把这些模式封装了。"

### Q2：为什么用 FastAPI？请求流程说一下

> "FastAPI 是 Python 的异步 Web 框架。流程是：前端 POST /api/chat 带 JSON 请求体 → FastAPI 用 Pydantic 校验参数 → 调 run_agent → Agent 循环跑完 → 返回 {"answer", "steps"}。选它是因为原生支持 async，适合 LLM 这种 IO 密集场景，还自带 /docs 交互式接口文档。另外我给项目加了 /health 健康检查接口，符合运维习惯。"

### Q3：工具是随便让 LLM 执行命令吗？安全怎么考虑？

> "不是。安全上我做了三点：① 工具只做**只读检测**（ping/DNS/socket/HTTP），没有任何写操作；② 工具函数是白名单——LLM 只能从我预定义的 TOOL_FUNCTIONS 字典里选，不能执行任意命令；③ subprocess 加了 timeout 防止挂死，System Prompt 里也要求对危险操作提醒风险。生产环境还会加权限控制和审计日志。"

## 三、其他高频追问（一句话答案）

| 问题 | 答法 |
|---|---|
| 为什么用 DeepSeek 不用 OpenAI？ | 它兼容 OpenAI SDK 格式，换模型只改 base_url 和 model 名两行，成本低；架构上模型是可替换的 |
| 后续怎么扩展？ | 加 Linux 命令解释工具、日志分析工具（正则识别错误码）、RAG 知识库（ChromaDB 存运维文档，先检索再回答） |
| RAG 了解吗？ | 检索增强生成：文档切块 → Embedding 成向量 → 存向量库 → 用户提问时先做相似度检索，把相关文档塞进 Prompt 让 LLM 结合资料回答，解决模型不知道企业内部知识的问题 |
| 遇到过什么问题？ | 准备一个真实案例：比如工具返回结果太长把上下文撑爆 → 截断到 800 字符；或 LLM 有时不调工具直接猜 → 优化 System Prompt 强制"需要数据必须调工具" |
| 前端为什么这么简单？ | 最小闭环目标是验证 Agent 链路，前端单文件够演示；生产会换 Vue3 组件化 + 流式输出（Streaming） |

## 四、现场演示流程（提前排练一遍）

1. 打开页面，输入 **"网站打不开了，帮我排查 example.com"**
2. 指着输出里的 **[Agent 排查过程]** 讲："看，它先 DNS 解析，再 ping，再检测 443 端口，最后 HTTP 检查——每一步是真实执行的工具，不是编的"
3. 再演示一个端口不通的例子（如 check_port 一个关掉的端口），展示它能根据结果定位到"服务没监听"
4. 收尾："这就是我说的 Agent 价值——结论基于真实检测结果，并且排查链路是可解释的"

## 五、你要提前做的准备（今天就做）

- [ ] 去 platform.deepseek.com 注册并充值最低额度（约 10 元），拿 API Key 填进 `backend/.env`
- [ ] 按 README 三步把项目跑起来，**自己完整操作一遍**（面试官可能让你现场跑）
- [ ] 打开 agent.py，逐行读懂那个 for 循环——这是唯一必须背下来的代码
- [ ] 把上面 Q1 的答法背熟，对着镜子讲一遍 30 秒介绍
