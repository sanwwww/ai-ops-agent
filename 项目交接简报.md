# AI Ops Agent · 项目交接简报（给任何对话/助手的标准上下文）

> **使用说明**：把这份文件整体发给新对话，并附一句："这是我的项目完整上下文，请基于它继续优化/开发/答疑，不要重新提问已写明的事实。"
> **最后更新**：2026-09-24 ｜ 项目状态：已公网上线。已完成：RAG 知识库、工程化/可观测性、效果评估（25 条测试集 100%）、多轮会话记忆；全部已推送部署（最新 commit 1e596e1）

---

## 1. 项目是什么

**AI 智能运维助手（Ops Agent）**：用自然语言描述 IT/网络故障 → Agent 自动调度真实检测工具（DNS/ping/端口/HTTP）按标准排查链路定位问题 → 基于真实结果给出结论，排查过程实时可视化。

- **在线演示**：https://ai-ops-agent-jmul.onrender.com
- **代码仓库**：https://github.com/sanwwww/ai-ops-agent（所有者 GitHub 用户名：sanwwww）
- **本质**：单目标的 Tool Calling 工具调用循环（感知-决策-行动闭环）+ RAG 知识库增强 + 多轮会话记忆（SQLite），不含任务规划/多 Agent——面试口径要主动说明这个边界
- **简历口径**：⚠️ 简历/对外只写演示地址，**不写 GitHub 仓库地址**（所有者只有这一个项目，不愿公开仓库关联）

## 2. 技术栈与架构

```
Python 3.13 + FastAPI + uvicorn          后端框架
DeepSeek（deepseek-chat，OpenAI 兼容 SDK）  LLM
手写 Agent 循环（未用 LangChain）           核心机制
ChromaDB + MiniLM(ONNX) + 混合检索         RAG 知识库（2026-09-18）
SQLite 会话持久化 + 上下文预算管理          多轮记忆（2026-09-24）
pytest 21 项（mock LLM 驱动 Agent 循环）    测试
25 条故障测试集 + 评估脚本                 效果评估（2026-09-24，通过率 100%）
结构化日志 + request_id 请求追踪           可观测性（2026-09-18）
原生 HTML/CSS/JS 单文件前端                暗色控制台 UI
SSE（Server-Sent Events）                 流式推送
GitHub + Render（免费实例）                托管与 CI/CD
```

数据流：`index.html → POST /api/chat/stream → FastAPI → Agent 循环（LLM↔tools.py 多轮，含 search_knowledge RAG 检索）→ SSE 事件流 → 前端逐字渲染 + 工具卡片`

## 3. 目录结构与文件职责

```
ai-ops-agent/
├── README.md / 面试准备.md / 面试话术.md     # 文档
├── 现场演示全流程.md / 项目竞争力分析-AI岗.md  # 演示脚本 / 竞争力分析
├── 项目文档.md                              # 所有者学习用：逐文件讲解+面试题库
├── .gitignore                               # venv/、__pycache__/、.env、chroma_db/、ops_memory.db、.pytest_cache/
└── backend/
    ├── main.py        # FastAPI 入口：/api/chat、/api/chat/stream(SSE)、/api/history、/health、/；request_id 追踪中间件；session_id 解析/历史注入/回写
    ├── agent.py       # ★核心：run_agent + run_agent_stream，均支持 history 参数注入多轮上下文；工具调用入日志
    ├── tools.py       # 5 个工具(4 只读检测 + search_knowledge RAG) + TOOLS_SCHEMA + TOOL_FUNCTIONS 白名单；ping 双降级(无 ping 命令→TCP 探测/GBK 编码兼容)
    ├── memory_store.py    # ★会话记忆：SQLite(sessions/messages 两表)，单条截 1500 字、注入最近 8 条（新增 2026-09-24）
    ├── knowledge_base.py  # ★RAG：三级检索(API向量/本地向量/关键词) + 混合检索 + 空库自愈
    ├── build_kb.py    # RAG 入库脚本：knowledge/*.md 按 "## " 切块 → ChromaDB
    ├── knowledge/     # ★RAG 语料：network.md + systems.md，共 19 个知识条目
    ├── evaluate.py    # ★评估器：跑批 testset.json，统计工具准确率/结论命中率/耗时 → evaluation_report.md
    ├── evaluation/    # testset.json(25 条 6 类) + evaluation_report.md + results.json
    ├── tests/         # pytest 21 项：test_agent / test_tools / test_main / test_memory
    ├── static/index.html  # 单文件前端（暗色控制台，sessionId 随会话保存/切换）
    ├── .env           # DEEPSEEK_API_KEY（本地）；线上用 Render 环境变量，绝不入库
    └── requirements.txt  # fastapi / uvicorn / openai / python-dotenv / chromadb（版本已锁定）
```

## 4. 核心机制（改代码前必读）

### 4.1 Agent 循环（agent.py）
- messages = [system(SYSTEM_PROMPT 运维人设+排查链路)] + history(可选，多轮记忆注入) + user(问题)；LLM 无记忆，全靠 messages 传上下文
- 每轮 `client.chat.completions.create(model, messages, tools=TOOLS_SCHEMA, stream=True)`
- 流式分片拼装：`tool_acc` 字典按 index 累积 tool_calls 的 id/name/arguments（分片到达）
- 有 tool_calls → append 助手决策消息 → 按名字查 `TOOL_FUNCTIONS` 执行 → 结果以 role:"tool"(带 tool_call_id) 写回 → continue 下一轮
- 无 tool_calls → yield done，返回
- **max_turns=6** 防死循环

### 4.2 SSE 事件协议（前端按此解析）
`event: <类型>\ndata: <JSON>\n\n`，类型：
- `status` {stage} ／ `delta` {text}(逐字) ／ `tool_start` {tool,args} ／ `tool_result` {tool,args,result} ／ `done` {steps,elapsed[,answer]} ／ `error` {answer}

### 4.3 工具（tools.py，全部只读安全）
`dns_lookup(domain)`、`ping_host(host)`、`check_port(host,port)`、`http_check(url)`、`search_knowledge(query)`（RAG）
- 白名单字典 TOOL_FUNCTIONS：LLM 只能点名，不能执行任意命令
- 结果截断到 800 字符防撑爆上下文；subprocess 带 timeout

### 4.5 RAG 知识库（2026-09-18 新增）
- 语料：`backend/knowledge/{network.md, systems.md}`，19 个知识条目（含 ICMP 禁用/502 定位/排查方法论等）
- 入库：`python build_kb.py`，按 `## ` 二级标题切块（语义完整），source/title 存 metadata 可溯源；重建式入库保证幂等
- 检索：`knowledge_base.py` → ChromaDB PersistentClient（落盘 backend/chroma_db，gitignore）
- **混合检索（Hybrid Search）**：MiniLM 是英文模型中文召回差 → 向量过召回 top6 后做关键词重排（英文/数字 token + 中文 2-gram 命中数 ×2 + 向量相似度），再取 top3，截断 1200 字符
- 降级策略：知识库为空/检索异常只返回提示语，Agent 主流程继续，不阻塞排查
- 部署：Render Build 命令改为 `pip install -r requirements.txt && python build_kb.py`（首启会下载 79MB ONNX 模型）；另有**保险丝**：应用运行时发现库为空会自动执行入库（knowledge_base._build_if_empty），即使忘改 Build 命令也能自愈，但首次请求会慢
- 免费实例 512MB 内存偏紧，若 OOM 降级方案为换 API 型 embedding（如硅基流动 bge-m3）

### 4.6 可观测性与测试（2026-09-18 新增）
- request_id 中间件：每请求分配唯一 ID，日志记录方法/路径/状态/耗时，响应头回写 X-Request-ID
- Agent 每次工具调用写日志（logger "ops-agent.agent"）
- pytest 13 项：Agent 循环 mock 测试（工具结果写回/未知工具安全/max_turns 防死循环）、RAG 检索质量与白名单一致性、API 限流/输入校验/SSE 协议格式；跑法：`python -m pytest tests/ -v`

### 4.7 效果评估体系（2026-09-24 新增）
- 语料：`backend/evaluation/testset.json`，25 条故障测试集（DNS 4 / 连通性 3 / 端口 4 / HTTP 4 / 方法论 8 / 复合排查 2），每条 = 输入问题 + 期望工具集合 + 期望结论要点（正则）
- 评估器：`backend/evaluate.py`，逐条真实调用 run_agent（真 LLM + 真工具），打分维度：①工具调用准确率（期望工具全中）②结论要点命中率（正则，≥60% 及格）③耗时；产出 `evaluation_report.md` + `evaluation_results.json`（bad case 明细）
- 跑法：`python evaluate.py`（全量约 3 分钟、DeepSeek 费用几分钱）；`--limit N` 调试
- **实测结果（2026-09-24）：通过率 100%（25/25），工具调用准确率 100%，结论要点命中 100%，单例均耗时 6.7s**
- **评估闭环案例（面试王牌）**：首轮 96%，kb-06（内存/cache 问题）LLM 凭记忆直接回答、未调知识库 → 收紧 SYSTEM_PROMPT（方法论类问题必须先检索）→ 复测 25/25。完整演绎"评估发现 bad case → 定位根因 → 修复 → 回归验证"

### 4.8 会话记忆 / 多轮上下文（2026-09-24 新增，P1）
- 存储：`backend/memory_store.py`，SQLite（backend/ops_memory.db，gitignore），sessions + messages 两表；生产可平滑换 PostgreSQL，接口不变
- 上下文策略：只持久化 user / assistant 最终回答（工具中间过程不入库，排查细节由前端浮窗+日志承担）；单条入库截断 1500 字符，注入上下文取最近 8 条（约 4 轮）——上下文预算管理
- 链路：前端每次请求带 session_id（首轮由服务端生成并在 done 事件回传）→ main.py 加载历史 → run_agent_stream(message, history=...) 注入 messages → 流结束写回本轮问答；新增 GET /api/history?session_id= 查询持久化消息
- 降级：存储层异常只记 warning，Agent 降级为单轮模式，主流程不中断
- 前端：单文件 index.html 同步改造——sessionId 随会话保存/切换（点历史会话继续追问时记忆跟着切），新会话换新 ID
- **端到端验证（2026-09-24）**：同一 session 三轮对话，第三轮 Agent 准确复述第一轮的检测目标与结论（www.baidu.com:443 可连接）；测试 21 项全过（新增 test_memory.py 8 项）
- 注意：Render 每次重新部署清空容器磁盘 → ops_memory.db 会重置（与 chroma_db 同理），运行期间持久

### 4.4 安全与限流（main.py）
- 每 IP 滑动窗口限流 10 次/分钟（429）；消息 ≤500 字符（400）
- API Key 只从环境变量读取；/health 健康检查；PORT 环境变量适配云平台

## 5. 前端功能清单（static/index.html，单文件）

暗色 HUD 控制台风：Orbitron/JetBrains Mono 字体（jsdelivr @fontsource）、切角按钮、辉光、背景网格、扫描线
- **SSE 流式渲染**：打字机 + 工具卡片实时出现（steps-toggle 按钮条 → 点击弹出 steps-pop 浮窗查看每步调用详情，再点关闭）
- 侧栏仪表盘：Agent 状态、真实会话统计（提问数/工具调用数/耗时）、历史会话（localStorage，存 8 条）、◧ 收起/展开按钮
- 三主题切换（夜间/日间/护眼，body[data-theme] CSS 变量 + localStorage 持久化）
- 开机动画（2.4s 自检日志，点击跳过）、Markdown 渲染（marked+DOMPurify）、代码块一键复制、示例问题 chips、移动端抽屉侧栏

## 6. 关键参数台账

| 项 | 值 |
|---|---|
| 模型 | deepseek-chat，base_url=https://api.deepseek.com |
| Agent 循环上限 | 6 轮 |
| 检测工具 | 5 个（4 网络检测全只读 + 1 个 RAG 检索） |
| RAG 库 | ChromaDB ops_kb 集合，19 条，检索 top3（过召回 6 条重排），结果截断 1200 字符 |
| 测试 | pytest 21 项全过（13 项 2026-09-18 + 8 项会话记忆 2026-09-24） |
| **效果评估** | 25 条测试集，2026-09-24 实测通过率 100%、工具准确率 100%、结论命中 100%、均耗时 6.7s（详见 4.7） |
| **会话记忆** | SQLite 持久化，注入最近 8 条历史（约 4 轮），单条截断 1500 字；GET /api/history 可查；存储异常自动降级单轮（详见 4.8） |
| 限流 | 每 IP 10 次/分钟；消息 ≤500 字 |
| 工具结果截断 | 800 字符 |
| 端口 | 本地 8000；Render 动态 $PORT |

## 7. 部署信息（Render）

- 服务：Web Service，**Root Directory = backend**，Build = `pip install -r requirements.txt && python build_kb.py`（⚠️ 2026-09-18 起需加 build_kb.py，否则线上知识库为空），Start = `uvicorn main:app --host 0.0.0.0 --port $PORT`，实例 Free
- 环境变量：`DEEPSEEK_API_KEY`（只在 Render 配置）
- **免费版 15 分钟无访问休眠**，唤醒约 50 秒（演示前预热）
- 国内直连 GitHub 常被重置：git 推送需开代理（本机代理口 127.0.0.1:7890，git 已配置仅对 github.com 走该代理）；Render 站点本身一般可直连
- ⚠️ Render 部署 ≠ Docker，对外口径禁止写 Docker/K8s/Prometheus

## 8. 当前 Git 状态（2026-09-24）

- 本地与 origin/main 完全同步（最新 commit 1e596e1），Render 已部署最新版
- push 用系统 git `"C:\Program Files\Git\cmd\git.exe"`（凭据在 Windows 凭据管理器；便携 git 无凭据助手读不到）；github.com 需开本机代理（127.0.0.1:7890）
- ⚠️ Render 每次重新部署会清空容器磁盘：chroma_db（知识库）和 ops_memory.db（会话）会重置——知识库有空库自愈保险丝（首次检索自动重建，约 69s）；会话库重建即空（不影响功能）
- 演示/面试前：打开链接预热时先随便问一个问题（触发建库），再开始正式演示

## 9. 已知边界与演进路线

边界：无任务规划；问答驱动（非告警驱动）；只覆盖网络层检测，摸不到服务器内部；example.com 等走 CDN 的目标会屏蔽 ICMP（ping 无回包属正常）；多模态不支持（deepseek-chat 纯文本模型）
已完成：✅ RAG 知识库（混合检索+三级降级）✅ 效果评估（25 条测试集 100%）✅ 会话记忆（SQLite+上下文预算）
演进（下一步优先级）：①接监控告警（Prometheus webhook 触发 Agent，从问答式变告警驱动）②知识库文档解析 pipeline（支持导入真实企业 PDF/Word 文档、切块参数实验）③多模态报错截图识别（演进思路：需换支持 tool calling 的 vision 模型如 Qwen-VL——注意会推翻"换模型只改两行"口径，面试只讲思路不实现）

## 10. 相关资产索引（所有者本机）

| 文件 | 内容 |
|---|---|
| `C:\Users\zjy14\Desktop\111\AI-Ops-Agent\` | 项目正式目录（Git 仓库根） |
| 同目录 `项目文档.md` | 所有者学习文档：逐文件讲解+小白概念课堂+分层面试题库（应对面试主用） |
| 同目录 `面试话术.md` / `面试准备.md` / `现场演示全流程.md` | 话术速查 / 演示流程（4 个场景） |
| 同目录 `项目竞争力分析-AI岗.md` | AI 岗校招视角竞争力分析 + P0-P3 迭代路线 |
| `C:\Users\zjy14\Desktop\111\张佳怡-简历主档案-MASTER.md` | 简历素材唯一源 |
| `C:\Users\zjy14\Documents\xwechat_files\...\2026-09\张佳怡-双选会-技术岗-修改版2.pdf` | 当前投递版简历（项目条目已是 RAG 版，待补评估/记忆两条） |

## 11. 给接手助手的工作约定

1. 改前端只改 `static/index.html`（单文件）；改后端逻辑主要在 `agent.py`
2. 每次改动：桌面仓库 commit → 所有者 push → Render 自动部署；验收方式 `curl /health` + 对 `/api/chat/stream` 发请求统计 SSE 事件
3. 数字口径以第 6 节为准，不得夸大；新功能先写进本文档再实现
4. 所有者偏好：直接执行、少确认；中文结构化输出；安全红线（只读工具/白名单/限流）不可破
