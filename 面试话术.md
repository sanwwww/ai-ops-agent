# Ops Agent 面试话术（终版，与线上项目完全对应）

> 项目链接：<https://ai-ops-agent-jmul.onrender.com>  
> 代码仓库：<https://github.com/sanwwww/ai-ops-agent>  
> 面试前 2 分钟先打开链接预热（免费实例休眠唤醒约 50 秒）

---

## ⭐ 结合简历的整体叙事（最重要的一段，先看这个）

### 黄金故事线：实习痛点 → 项目动机（30 秒版）

> "我去年在一家金属材料公司做信息化运维实习，独立处理了 30 多起故障，建立了'受理→定位→处理→台账→复盘'的闭环。过程中我发现：**大部分时间都耗在'定位'这一步**——员工说'上不了网'，我要按 DNS、连通性、端口、服务一层层人工排查，新人最怕的就是不知道从哪查起。  
> 所以我用 Python + FastAPI 做了这个 AI 智能运维助手：把标准排查链路写成 4 个检测工具，让大模型通过 Agent 循环自动调度执行，**把实习时 10 分钟的人工初判压缩到 30 秒**，排查过程实时可见、可追溯。项目已部署上线，代码开源在 GitHub。"

这段话的威力：

- 项目动机不是"练手"，是**解决我亲身体验过的痛点**（简历上 30+ 故障闭环背书）
- 展示闭环思维：实习建立流程 → 项目把流程中最耗时的环节自动化 → "发现重复工作就自动化"的运维素养
- 把"运维实习"和"AI 项目"从两条孤立经历，拧成一条成长线

### 按岗位调整侧重点

| 岗位类型      | 强调重点                                                |
| --------- | --------------------------------------------------- |
| 运维 / 技术支持 | 实习 30+ 故障闭环的真实经验 + AIOps 演进思路（接告警→接服务器→接知识库）        |
| 解决方案 / 售前 | "把技术讲给外行听"：博物院 2000+ 人次讲解功底 + 现场演示项目；工具可扩展=可按客户场景定制 |
| 偏开发的岗     | 手写 Agent 循环、SSE 流式架构、限流设计、CI/CD 自动部署                |

### 简历项目描述建议升级（2026-09-18 RAG 版）

建议替换为：

> **AI 智能运维助手（Ops Agent）** · 个人项目 · 2026.05–至今  
> · 独立设计并**部署上线** LLM 运维 Agent（公网可访问）：手写 Tool Calling Agent 循环，LLM 负责决策、代码负责执行，自动调度 DNS / 连通性 / 端口 / HTTP 检测及**知识库检索（RAG）**&#x4E94;类工具，按标准排查链路自动定位故障  
> · 搭建 **RAG 运维知识库**：19 篇排障文档按语义切块入 ChromaDB，针对英文 Embedding 模型中文召回差的问题实现**混合检索（向量召回 + 关键词重排）**，知识库以降级模式接入 Agent 主流程  
> · 实现 SSE 流式输出与请求链路追踪（request_id + 结构化日志），编写 **pytest 单元测试 13 项**（Agent 循环 mock 测试 / 工具白名单 / API 限流与 SSE 协议）；建立公网安全防护（IP 限流、输入校验、密钥环境变量管理）  
> · 建立**效果评估体系**：25 条故障测试集（6 类）+ 评估脚本跑批，实测**排查通过率 100%、工具调用准确率 100%**，并通过 bad case 分析修复知识库调用不稳定问题，形成"评估 → 定位 → 修复 → 回归"闭环  
> · 实现**多轮会话记忆**：SQLite 持久化 + 上下文预算管理（历史轮数/长度上限、只存有效信息），支持跨轮故障追踪追问，存储异常自动降级单轮  
> · 搭建 GitHub + Render CI/CD，push 自动构建部署；附演示链接

把两个链接加进简历——**面试官扫码就能现场玩你的项目**，这是应届生简历里几乎没有的杀手锏。

### 简历其他条目与项目的联动话术

- 遇到"你有什么运维经验" → 实习 30+ 故障闭环（讲 1 个具体案例：受理→定位→处理→台账）+ "所以我把定位环节自动化了"
- 遇到"你怎么理解解决方案岗" → "就是把技术讲成业务价值——我讲解 2000+ 人次练的是这个，这个项目的演示设计（暗色控制台、实时排查可视化）也是给非技术决策者看的"
- 遇到"团队协作" → 智慧出行 App 7 人负责人：定接口规范、代码评审 → "这个项目我单干，但我把当时定的工程规范（安全边界、留痕、文档）都带过来了"

---

## 一、30 秒开场介绍（必背，脱口而出）

> "我做了一个 AI 智能运维助手 Ops Agent，解决的实际问题是：运维新人遇到'网站打不开'这类故障时，不知道按什么顺序排查。  
> 它的架构是：FastAPI 做后端，接 DeepSeek 大模型，我**手写了 Agent 循环**——大模型负责决策，我的代码负责执行 DNS 解析、ping、端口检测、HTTP 检查这些真实工具，另外还接了一个 **RAG 运维知识库**：模型需要排查经验时自动检索知识库，把'凭模型记忆回答'变成'结合沉淀的运维知识回答'。检测结果实时喂回模型，让它像有经验的工程师一样按照'DNS → 连通性 → 端口 → HTTP'的标准链路逐层排查，最后基于**真实检测数据**给出结论，而不是像普通聊天机器人那样凭空给建议。  
> 前端是暗色控制台风格，用 SSE 流式输出，排查过程一步步实时可见。项目已经部署上线，可以直接体验，代码在 GitHub 上。"

## 二、被要求"详细讲讲"时的 2 分钟展开（按这五点讲）

**1. Agent 循环（核心，讲最久）**

> "整个项目的灵魂是一个几十行的循环：我把系统提示词和用户问题放进 messages 列表，每次循环把完整对话历史加上工具清单发给大模型。模型只负责决策——需要真实数据就返回 tool_call，我的代码在预定义的函数表里按名字找到对应函数执行，把结果写回 messages 进入下一轮；模型认为信息够了就输出最终结论。最多循环 6 次防止死循环。两个关键认知：**LLM 没有记忆，全靠 messages 传递上下文；LLM 只决策不执行，工具执行和安全控制都在我的代码里**。"

**2. 为什么手写不用 LangChain**

> "我特意没用框架。手写循环让我真正理解了 Tool Calling 的机制——工具的 JSON Schema 怎么给模型看、tool_call 分片怎么拼装、tool_call_id 怎么对账。理解了原理之后，LangChain 只是把这些模式封装了，随时可以切换。"

**3. SSE 流式输出**

> "排查过程和回答都是实时推给前端的。我用 SSE（Server-Sent Events）：后端把 Agent 循环的每个事件——工具开始调用、工具结果、生成的文字片段——作为事件流推给前端，前端逐字渲染。相比 WebSocket，SSE 单向推送更轻量，浏览器原生支持，特别匹配'Agent 边查边汇报'的场景。"

**4. RAG 知识库（加分项，AI 岗必聊）**

> "知识库解决的是'模型不知道怎么解读检测结果'的问题：我写了 19 篇运维排障文档，按二级标题切块（保证每块是一个完整的排查思路），Embedding 后存进 ChromaDB。对 Agent 来说它就是一个普通的只读工具 search_knowledge，模型在不确定下一步排查方向时自己决定调用。有一个真实的技术坑：内置 Embedding 模型是英文的，中文检索不准，我做了混合检索——向量召回 + 关键词重排——才把准确率拉起来。设计上知识库是降级模式接入的：检索挂了只影响这一步，Agent 主流程继续用检测工具排查。"

**5. 安全设计（运维岗必聊）**

> "三道防线：第一，工具全部是**只读检测**，没有任何写操作；第二，函数白名单——模型只能从我预定义的字典里选工具，不能执行任意命令；第三，因为部署在公网，我加了滑动窗口限流（每 IP 每分钟 10 次）和消息长度限制，防止 API Key 被刷爆。Key 只存在平台环境变量里，代码仓库里永远不出现。"

**6. 部署与 CI/CD**

> "代码在 GitHub，用 Render 做 CI/CD——push 到 main 分支自动构建部署，零运维成本。后端通过 PORT 环境变量适配云平台动态端口。"

## 三、高频追问速查表

| 面试官问                      | 你答                                                                                                                                                                                                                                                                                                  |
| ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 和 ChatGPT 套壳的区别？          | 结论基于真实检测结果 + 排查链路可解释（前端能看到每步调了什么工具、结果是什么）+ 工具安全白名单                                                                                                                                                                                                                                                  |
| 为什么用 DeepSeek？            | 兼容 OpenAI 协议，换模型只改 base_url 和模型名两行，模型层可替换；成本低                                                                                                                                                                                                                                                       |
| **RAG 是怎么做的？（必问）**        | "我先说为什么需要：模型不了解运维排障的**方法论**，检测到现象后怎么解读、下一步查什么，这些经验要外部注入。我建了 19 篇运维知识文档，按二级标题切块——因为一个标题下的内容就是一个完整排查思路，语义不会断——切块后用 Embedding 模型向量化存进 ChromaDB。对 Agent 来说知识库就是一个新工具：模型不确定下一步怎么查时调用 search_knowledge，我检索出最相关的 3 个片段作为工具结果喂回去，模型结合真实检测结果作答。**关键是 RAG 在我架构里是增强不是依赖**——检索挂了只返回提示语，Agent 继续用检测工具排查，主流程不中断。" |
| **Embedding 用的什么？遇到什么坑？** | "用的 ChromaDB 内置的 all-MiniLM-L6-v2。踩过一个真实的坑：这个模型是英文模型，中文检索效果差——我测'网站报 502 怎么排查'，纯向量召回命不中 502 那篇。我的解法是**混合检索（Hybrid Search）**：向量先过召回 6 条，再用关键词重排——从查询里抽错误码、协议名和中文 2-gram，统计在文档里的命中数加权打分。上线后 502 查询精准命中'HTTP 5xx 定位'和'Nginx 故障'两条。这件事让我理解了为什么生产环境常用 bge-m3 这类中文模型，以及为什么 RAG 检索层必须可评测、可迭代。"             |
| **测试怎么做的？（升级版，必问）**       | "pytest 写了 13 个单测，分三层：Agent 循环层用 mock 的假 LLM 客户端跑完整循环——验证工具结果写回、白名单外工具拒绝执行、max_turns 防死循环；工具层验证 RAG 检索质量和白名单一致性；API 层用 FastAPI TestClient 测限流、输入校验、SSE 事件协议格式。**原则是：Agent 循环逻辑必须可测，LLM 本身不可控所以隔离掉**——这是 Agent 项目测试的标准做法。"                                                                           |
| **怎么评估 Agent 效果？（必问，有数字）** | "我建了 25 条故障测试集，覆盖 DNS/连通性/端口/HTTP/方法论/复合排查六类，每条定义了期望调用的工具集合和期望结论要点。评估脚本逐条真实调用 Agent 跑批，统计三个指标：工具调用准确率、结论要点命中率、耗时。**最新一轮实测：通过率 100%，工具准确率 100%，平均 6.7 秒一条。**最有价值的其实是失败的那一条：首轮 96%，一个内存/cache 问题模型觉得'我自己会'就跳过了知识库检索——我收紧了系统提示词里'方法论类问题必须先检索'的约束，复测就满分了。这个过程就是评估的真正价值：**没有评测集，你永远不知道模型什么时候在凭感觉回答**。" |
| **多轮记忆怎么做的？（P1 新增，必问）** | "LLM 本身无状态，我在服务端加了会话存储：前端每个会话带 session_id，SQLite 持久化 user/assistant 问答，下一轮把最近 8 条历史注入 Agent 的 messages 上下文——因为 LLM 的'记忆'本质上就是上下文拼接。两个工程细节：①只存最终回答、工具中间过程不入库，上下文窗口留给有效信息；②单条截断 + 轮数上限做上下文预算管理，防止多轮对话 token 越滚越大。实测三轮对话后 Agent 能准确复述第一轮的检测目标和结论。存储层挂了自动降级单轮，不阻塞主流程。" |
| 日志和线上排查？                  | 每个请求有中间件分配唯一 request_id，写进日志和响应头 X-Request-ID，出问题用户报 ID 就能定位整条请求日志；Agent 每次工具调用也入日志，调用链路可回溯                                                                                                                                                                                                         |
| 遇到过什么问题？                  | 准备四个真实案例：①评估发现知识库调用不稳定（模型凭记忆回答）→ 收紧 prompt 约束 → 复测 100% ②英文 embedding 模型中文召回差 → 混合检索重排解决 ③工具返回结果太长会撑爆上下文 → 截断 ④容器无 ping 命令 → TCP 探测降级 |
| 并发/性能怎么考虑？                | FastAPI 异步架构适合 IO 密集；限流保护后端；LLM 调用是瓶颈，生产可以加队列和缓存                                                                                                                                                                                                                                                    |
| 后续怎么扩展？                   | ①监控告警触发（Prometheus webhook 直接唤起 Agent，从问答式变告警驱动）②多轮会话记忆持久化 ③知识库内容按真实故障复盘持续扩充                                                                                                                                                                                                                        |

## 四、现场演示流程（3 分钟，提前排练）

1. 打开链接 → **开机动画**自然带出："这是一个运维控制台风格的设计"
2. 输入"网站打不开了，帮我排查 example.com" → 指着实时出现的工具卡片讲：
   > "看，它在按标准链路自动排查：先 DNS 解析、再 ping、再查端口、最后 HTTP 检查——每一步都是真实执行的工具，不是模型编的"
3. 点开"Agent 排查过程"浮窗 → "每一步的调用参数和原始结果都可追溯、可解释"
4. 演示第二个案例：问一个不通的端口 → 展示它能定位到"端口不通/服务未监听"
5. 收尾："这就是 Agent 的价值——**结论基于真实数据，过程可解释，工具可管控**"

## 五、收尾金句（面试官说"不错"之后）

> "这个项目让我把网络工程的排查经验和大模型应用结合了起来。我认为运维 Agent 落地的关键不是模型多强，而是三件事：**给模型可靠的工具、约束它的执行边界、让排查过程可解释**——这三点我都落地了。"

## 六、面试前检查清单

- [ ] 提前 2 小时：打开一次线上链接（确认还活着 + 预热）
- [ ] 提前 10 分钟：再打开一次（防止刚好休眠）
- [ ] 本地兜底：万一线上打不开，`python main.py` 本地跑，效果一样
- [ ] 把两个链接存在手机备忘录，方便现场发给面试官
- [ ] 背熟 30 秒介绍；Agent 循环对着 agent.py 再读一遍
