# Ops Agent 项目竞争力分析（面向 AI 应用工程师 / AI Agent 开发校招岗）

> 分析日期：2026-09-24 ｜ 分析视角：校招 AI 岗面试官 + 求职教练  
> 依据：项目实际代码与线上状态（2026-09-18 版）+ 张佳怡简历（技术岗修改版2）  
> 说明：未提供具体 JD，本报告按行业招聘常识提炼能力画像；拿到目标公司 JD 后可精调

---

## 一、岗位能力画像（评判标准）

| # | 能力项                                      | 来源   |
| - | ---------------------------------------- | ---- |
| ① | LLM 应用核心：Tool Calling / Agent 循环 / 上下文工程 | 行业常识 |
| ② | RAG 全链路：切块、Embedding、向量库、检索优化            | 行业常识 |
| ③ | 工程化：API 设计、测试、日志追踪、部署 CI/CD              | 行业常识 |
| ④ | 会话记忆 / 多轮上下文管理                           | 行业常识 |
| ⑤ | 任务规划 / 长任务拆解（Planning）                   | 行业常识 |
| ⑥ | 效果评估与迭代（评测集、指标、bad case 分析）              | 行业常识 |

校招 AI 应用岗的隐含期待：**讲得清原理 + 有落地证据 + 知道怎么衡量好坏**。前两条你已有，第三条是当前断档。

## 二、优点（面试官视角的真实加分项）

1. **手写 Agent 循环，不套框架**——分片拼装 tool_calls、tool_call_id 对账、max_turns 防死循环，这些细节被深挖时答得出来。校招项目 90% 是 LangChain 套壳，你的差异点在这里。
2. **RAG 不是 demo 级，有真实迭代故事**：英文 Embedding 模型中文召回差 → 混合检索（向量召回+关键词重排）；三级降级（API 向量/本地向量/关键词）体现资源权衡思维。这是能在面试里讲 5 分钟不重样的素材。
3. **工程化超出应届平均线**：13 项 pytest（含 mock LLM 测 Agent 循环——这是 Agent 项目测试的标准做法）、X-Request-ID 请求追踪、限流/白名单安全、CI/CD 自动部署。
4. **真实公网运行 + 自愈能力**：空库自动重建、容器无 ping 自动 TCP 探测降级——"工具失效不让服务失效"是生产思维，面试官认这个。
5. **与简历整体叙事形成闭环**：网络工程专业 → 5 个月运维实习（30+ 故障闭环）→ 把实习中最耗时的"定位"环节做成 Agent。动机真实，不是"为了做项目而做项目"。
6. **公网演示链接是杀手锏**：面试官扫码就能玩，双非本科简历里几乎没有第二个。

## 三、缺点与风险（面试官会戳的地方）

1. **【最大短板】零效果评估**：没有测试集、没有准确率数字。简历上全是"做了什么"，没有"做得怎么样"。AI 岗面试必问"你怎么评估 Agent 好不好"，目前只能答设计思路，没有实证。
2. **单轮问答，无会话记忆**：用户追问"刚才那个域名呢"接不上。Agent 五要素（感知-规划-工具-记忆-执行）里"记忆"是空白，只能谈设计。
3. **无任务规划**：单目标循环，没有 Plan-and-Execute / 任务拆解。好在简报口径已备（"单目标 Tool Calling 循环，不含规划/多 Agent"），主动说边界可守住。
4. **知识库规模小（19 条自写语料）**：数据真实但单薄；没有真实企业文档导入 pipeline（PDF/Word 解析、分块参数实验）。被问"生产环境文档量大怎么办"只能谈方案。
5. **技术栈单一**：只接 DeepSeek；无多模型对比/路由；无 LangGraph/LlamaIndex 框架经验（可用"原理在手，框架随时切换"应对，部分 JD 明确要求框架时吃亏）。
6. **简历 AI 关键词偏薄**：技能标签只有"Tool Calling / SSE / Agent 开发"，缺 RAG、ChromaDB、Embedding、向量检索、pytest、CI/CD 等高频检索词——机器筛和 HR 扫读都可能漏掉你的 RAG 实力。

## 四、提升迭代优先级（按秋招 ROI 排序）

### P0：效果评估体系（1-2 天，强烈建议立刻做）

- 建 20-30 条故障测试集：输入问题 + 期望调用的工具序列 + 期望结论要点
- 写评估脚本跑批（调线上 API），统计：排查结论正确率、工具调用序列准确率、平均耗时
- 简历直接多一条硬数字："在 25 条故障测试集上排查准确率 XX%，工具调用准确率 XX%"
- 顺带产出 bad case 分析表 → 面试讲"我发现 X 类问题容易误判，加了 Y 修复"——这是 Agent 岗面试最值钱的段落

### P1：会话记忆 + 多轮排查（1 天）

- SQLite 存会话历史，前端带 session_id，追问能接上下文
- 补齐 Agent"记忆"要素，简历可加"支持多轮上下文的故障追踪"

### P2：知识库数据 pipeline（可选，2-3 天）

- 支持导入真实 PDF/Markdown 运维文档（pdfplumber 解析 + 切块参数对比实验）
- 应对"生产环境文档量大、格式杂怎么办"

### P3：了解即可、面试能讲不用做

- LangGraph 状态机迁移思路、Langfuse 全链路追踪、Prometheus webhook 告警驱动、多模型路由

### 简历层联动（改技能标签，5 分钟）

技能标签建议补：`RAG · ChromaDB · 向量检索 · pytest · CI/CD · AIOps`——原词进简历，ATS 命中率直接提升。

## 五、面试口径提醒（守住边界就是得分）

- 主动说："这是单目标的 Tool Calling 循环，不含任务规划和多 Agent——我的理解是先把手写循环吃透，框架只是这些模式的封装"
- 评估问题在 P0 做完前，标准答案是："我正在建评测集，目前的评估方式是人工验收 + 单元测试，我认可没有评估的 Agent 等于黑盒"——诚实 + 有方法论意识
- 被问 ML 基础（训练/微调）：校招 AI 应用岗不深究，但要能讲清 Prompt/微调/RAG 三者的适用边界

---

*下一步建议：先做 P0（我可以直接开工写测试集和评估脚本）；拿到目标公司 JD 后发我，我按 JD 精调简历与面试题。*
