# Ops Agent 项目文档（学习版 · 面试备战）

> **这份文档是给谁的**：给项目所有者（张佳怡）自己看的。目标：读完能完整理解项目每一层在干什么、为什么这么设计，面试被问到任何一层都能答。
> **配套文件**：面试话术.md（速查表）/ 现场演示全流程.md（演示脚本）/ 项目交接简报.md（技术事实档案）
> **更新**：2026-09-24，与线上版本（commit 1e596e1）一致

---

## 一、项目是什么（一句话 + 大白话类比）

**一句话**：一个部署在公网的 AI 运维助手——用户用中文描述"网站打不开"这类故障，Agent 自动调度真实检测工具（DNS/ping/端口/HTTP）按标准排查链路定位问题，结论基于真实检测结果而非模型想象，排查过程实时可视化。

**大白话类比**：想象一个 7×24 小时在线的运维老师傅。
- 你说"我上不了网"，他不会直接瞎猜，而是**先动手查**：先看域名能不能解析（DNS），再 ping 一下通不通，再查端口开没开，再看 HTTP 服务有没有响应——一层层排除；
- 遇到"502 是什么原因"这种经验题，他会**翻自己的排查手册**（知识库）再回答；
- 你昨天问过什么，他**记得**（会话记忆）；
- 他每查一步都会**把检测报告摆在你面前**（工具卡片实时可视化），你能看到他每一步调了什么工具、拿到了什么数据。
- 最关键的：这个"老师傅"只有**诊断权没有处置权**——他只做只读检测，不能执行任何危险命令。

**在线演示**：https://ai-ops-agent-jmul.onrender.com（免费实例 15 分钟无访问会休眠，唤醒约 50 秒）

---

## 二、项目能做什么（功能清单）

| 功能 | 说明 |
|---|---|
| 故障自动排查 | 输入故障描述 → Agent 自动决定查什么、按什么顺序查 → 给出"当前判断 + 可能原因 + 下一步命令" |
| 真实检测 | DNS 解析、ping/TCP 连通性、TCP 端口、HTTP 状态码，全部真实执行，结果可溯源 |
| RAG 知识库 | 19 篇运维排障经验文档，"502 怎么排查/CPU 飙高思路"这类经验题先检索再回答 |
| 多轮记忆 | 同一会话内追问"刚才查的是哪个网站"，Agent 记得（SQLite 持久化） |
| 流式可视化 | SSE 逐字输出 + 每步工具调用实时出现卡片，点开浮窗可看每步的参数和原始结果 |
| 效果可量化 | 25 条故障测试集自动跑批评估，实测通过率 100% |
| 公网安全 | IP 限流（10 次/分钟）、消息长度限制、工具白名单、API Key 环境变量管理 |

---

## 三、技术栈总览（用了什么、为什么选它）

| 技术 | 干什么 | 为什么选它（面试答法） |
|---|---|---|
| Python 3.13 | 主语言 | 生态最全，LLM 应用首选 |
| FastAPI + uvicorn | Web 框架 | 原生异步，适合 IO 密集场景（等 LLM 响应、等网络检测都是 IO） |
| DeepSeek（deepseek-chat） | 大模型 | OpenAI 协议兼容——**换模型只改 base_url 和模型名两行**，模型层可替换；成本低 |
| 手写 Agent 循环 | 核心机制 | **特意不用 LangChain**：手写才能真正理解 Tool Calling 原理（工具 Schema 怎么给模型看、分片怎么拼装、tool_call_id 怎么对账）；理解原理后框架只是封装 |
| ChromaDB | 向量数据库 | 轻量、可嵌入式部署（不用单独起服务），PersistentClient 数据落盘 |
| all-MiniLM-L6-v2 (ONNX) | Embedding 模型 | ChromaDB 内置、零配置；中文效果差的坑用混合检索补（这本身就是面试素材） |
| SQLite | 会话存储 | 零运维依赖，够用；生产可平滑换 PostgreSQL，接口不变 |
| SSE (Server-Sent Events) | 流式推送 | 比 WebSocket 轻：单向推送够用（服务端→前端），浏览器 fetch 原生支持 |
| pytest | 单元测试 | 21 项，Agent 循环用 mock 假 LLM 测试——**循环逻辑必须可测，LLM 本身不可控所以隔离掉** |
| GitHub + Render | 托管 + CI/CD | push 自动构建部署，零运维成本；免费实例适合个人项目 |

---

## 四、架构与一次请求的完整生命周期

```
用户输入（前端 index.html）
    │  POST /api/chat/stream  {message, session_id}
    ▼
FastAPI (main.py)
    │  ① 限流检查（每 IP 10 次/分）② 消息长度校验（≤500 字）
    │  ③ 按 session_id 从 SQLite 加载最近 8 条历史
    │  ④ 生成 request_id（响应头 X-Request-ID 回传）
    ▼
Agent 循环 (agent.py)  ←—— 这就是"感知-决策-行动"闭环
    │  messages = [system 提示词] + [历史对话] + [用户问题]
    │  ┌────────────── 循环（最多 6 轮）──────────────┐
    │  │ 把 messages + 工具清单发给 LLM               │
    │  │   ├── LLM 返回 tool_call → 我的代码执行工具  │
    │  │   │    → 结果写回 messages → 下一轮          │
    │  │   └── LLM 直接回答 → 结束循环               │
    │  └─────────────────────────────────────────────┘
    │  每一步都通过 yield 发事件
    ▼
SSE 事件流 → 前端逐字渲染 + 工具卡片实时出现
    │  done 事件：本轮问答写回 SQLite（记忆）
    ▼
用户看到结论
```

**走一遍具体例子**：用户输入"帮我查 github.com 的 443 端口通不通"
1. 前端把消息 + session_id POST 给后端
2. 后端校验限流/长度，从 SQLite 取出这个会话的历史（比如第一轮问过 502）
3. Agent 第 1 轮：LLM 看到 5 个工具的说明书，决定"这题要真实数据"→ 返回 tool_call: `check_port(host="github.com", port=443)`
4. 代码在白名单字典里找到 check_port 函数执行，真实发起 TCP 连接，拿到"端口正常：可连接"
5. 结果写回 messages，第 2 轮：LLM 认为信息够了 → 输出最终结论（基于真实结果，不编造）
6. 期间每个事件（工具开始/结果/文字片段）实时推给前端
7. 结束后：本轮问答存入 SQLite，done 事件把 session_id 回传给前端记住

---

## 五、核心概念小白课堂（面试前必读，按顺序看）

1. **LLM（大语言模型）**：本质上是个"根据上文预测下一个词"的函数。它没有记忆、不会执行任何操作，只会生成文字。
2. **上下文（Context）/ Token**：LLM 每次调用都要把"全部对话内容"发给它，这就是上下文。上下文越长越贵越慢——所以项目里到处都是"截断"（工具结果 800 字、知识库 1200 字、历史消息 1500 字）。
3. **Tool Calling（工具调用/函数调用）**：OpenAI 协议定义的机制——把工具的"说明书"（JSON Schema：名字、功能描述、参数）随请求发给模型，模型需要真实数据时不直接回答，而是返回一个 tool_call（"我要调 xxx 工具，参数是 yyy"）。**注意：模型只是"点名"，真正执行工具的是你的代码**——这是安全控制的根本。
4. **Agent 循环（ReAct）**：LLM 思考 → 决定调工具 → 代码执行 → 结果喂回 → 再思考 → …… → 最终回答。Agent 和普通聊天机器人的区别就在这个循环：它能"决定下一步做什么"。
5. **SSE（Server-Sent Events）**：服务器向浏览器单向推送数据的协议。Agent 边查边汇报，正好匹配"事件流"：status/delta(逐字)/tool_start/tool_result/done/error 六种事件。
6. **RAG（检索增强生成）**：模型不知道你公司内部的排障经验 → 把经验文档切块、向量化存进向量库 → 用的时候按语义检索最相关的几条塞给模型作答。"增强"而非"依赖"：检索挂了主流程照跑。
7. **Embedding / 向量库**：把文字变成一串数字（向量），语义相近的文字向量距离近。向量库就是能按"语义相似度"查文档的数据库。
8. **混合检索（Hybrid Search）**：纯向量检索对中文效果差（我们用的 MiniLM 是英文模型），所以向量召回 6 条后，再用关键词命中数重排（从查询里抽"502""DNS"和中文 2-gram），取前 3 条。
9. **会话记忆**：LLM 无状态，"记忆"= 把历史对话拼进上下文。要点是**预算管理**：只存有效问答（不存工具中间过程）、单条截断、只取最近 8 条。
10. **pytest / mock**：单元测试框架；mock = 用一个"假的"替身替换真实依赖。测 Agent 循环时把 LLM 换成假客户端（固定返回 tool_call 或固定回答），这样循环逻辑可以自动化测试且不花钱不随机。
11. **CI/CD**：push 代码到 GitHub → Render 自动拉代码、装依赖、启动服务。部署即上线。

---

## 六、逐文件讲解（每个文件干什么）

### backend/main.py —— 门面（Web 层）
- 四个接口：`POST /api/chat`（非流式，保留兼容）、`POST /api/chat/stream`（SSE 主接口）、`GET /api/history`（查会话消息）、`GET /health`（健康检查，顺带暴露知识库状态）
- **请求追踪中间件**：每个请求生成 12 位 request_id → 日志记录方法/路径/状态/耗时 → 响应头 `X-Request-ID` 回传。线上出问题用户报 ID 就能定位整条请求日志
- **限流**：每 IP 滑动窗口 10 次/分钟（429）；消息 ≤500 字（400）——公网部署防 API Key 被刷爆
- **会话链路**：解析 session_id（没传就生成）→ 加载历史 → 传给 Agent → 流结束后把本轮问答写回 SQLite → done 事件带回 session_id

### backend/agent.py —— 灵魂（Agent 循环）
- `run_agent`（非流式兜底）+ `run_agent_stream`（SSE 流式主用），都支持 history 参数
- SYSTEM_PROMPT：运维人设 + 六条工作规则（先分类 → 真实检测 → 标准链路 → 方法论必查知识库 → 结构化结论 → 危险操作提醒）
- 流式难点：LLM 流式返回时 tool_call 是**分片到达**的（参数 JSON 被切成碎片），用 tool_acc 字典按 index 拼装
- max_turns=6 防死循环；每次工具调用写日志

### backend/tools.py —— 手（工具层）
- 5 个工具：`dns_lookup` / `ping_host` / `check_port` / `http_check` / `search_knowledge`
- **白名单机制**：TOOLS_SCHEMA 是给 LLM 看的说明书；TOOL_FUNCTIONS 是"名字→函数"字典，LLM 只能点名字典里有的，**传什么都执行不了任意命令**
- ping_host 双降级（踩坑修复）：①容器（Render）没装 ping 命令 → FileNotFoundError 时自动改 TCP 443/80 探测；②中文 Windows 的 ping 输出是 GBK 编码 → 显式指定编码防解码崩溃
- 结果截断 800 字符防撑爆上下文；subprocess 带 timeout

### backend/knowledge_base.py + build_kb.py + knowledge/ —— 参考书（RAG 层）
- 语料：knowledge/ 下两个 md 文件，19 个知识条目（502/5xx/4xx 定位、CPU/内存/磁盘排查、Nginx、MySQL、日志分析、排查方法论、工具使用边界）
- build_kb.py：按 `## ` 二级标题切块（一个标题 = 一个完整排查思路，语义不断裂），source/title 存 metadata 可溯源，重建式入库幂等
- knowledge_base.py：三级检索策略——①配了硅基流动 Key 走 bge-m3 API 向量（中文好、零内存）②没 Key 用本地 MiniLM ③向量挂了自动永久降级纯关键词检索；**空库自愈保险丝**：检索时发现库为空自动重建
- 检索流程：向量召回 6 条 → 关键词重排打分（命中数×2 + 向量相似度）→ 取 top3 → 截 1200 字

### backend/memory_store.py —— 记忆（会话层）
- SQLite 两张表：sessions（会话）+ messages（消息）
- 只存 user/assistant 最终回答；单条截 1500 字；注入取最近 8 条（约 4 轮）

### backend/evaluate.py + evaluation/ —— 考官（评估层）
- testset.json：25 条测试集，每条 = 输入 + 期望工具集合 + 期望结论要点（正则）
- evaluate.py：逐条真实调用 Agent，算三个指标（工具调用准确率 / 结论要点命中率 / 耗时），产出 Markdown 报告 + bad case 明细
- 跑法：`python evaluate.py`（约 3 分钟）；`--limit N` 调试

### backend/static/index.html —— 脸（前端，单文件 545 行）
- 暗色控制台风（HUD/辉光/扫描线）、开机自检动画、三主题切换
- SSE 解析：按 event 类型分别处理，delta 逐字渲染、工具卡片实时出现、浮窗看每步详情
- 历史会话存 localStorage（8 条），sessionId 跟随会话切换

### backend/tests/ —— 质检（21 项测试）
- test_agent.py：用假 LLM 客户端跑完整循环——验证工具结果写回、白名单外工具拒绝执行、max_turns 防死循环、无 Key 友好报错
- test_tools.py：RAG 检索质量（"502 排查"必须命中 502 文档）、白名单一致性
- test_main.py：限流、消息长度、SSE 事件协议格式、X-Request-ID
- test_memory.py：记忆追加/读取/截断/上限、两轮对话共享上下文、不同会话隔离、/api/history

---

## 七、关键设计决策与"为什么"（面试最爱问的是 Why）

| 决策 | 为什么 |
|---|---|
| 手写循环不用 LangChain | 理解原理 > 会用框架；原理懂了框架随时切换 |
| RAG 做成"工具"而不是前置过滤 | 对 Agent 来说知识库只是 5 个工具之一，模型自己决定什么时候查——架构统一，且检索挂了不影响主流程 |
| 混合检索而不是换更好的 embedding | 换 API embedding 要多一个外部依赖（Key、网络、费用）；混合检索零依赖解决 90% 问题，且 bge-m3 路线已预留（三级降级第①级） |
| 会话只存问答不存工具过程 | 上下文窗口是稀缺资源；排查细节的可追溯性由前端浮窗 + 日志承担，各司其职 |
| 评估用关键词正则而不是 LLM 打分 | 确定性、可复现、零成本；期望结论要点本来就是结构化的 |
| 一切降级设计 | RAG 挂了→提示语继续排查；记忆挂了→单轮模式；ping 没有→TCP 探测。**核心思想：增强组件的失败不能传导到主流程** |

---

## 八、数字台账（背下来，被问数字时不能卡壳）

| 项 | 值 |
|---|---|
| 模型 | deepseek-chat（OpenAI 兼容协议） |
| Agent 循环上限 | 6 轮 |
| 工具 | 5 个（4 检测全只读 + 1 知识库检索），白名单 |
| 知识库 | 19 条，切块按二级标题，检索 top3（向量召回 6 → 关键词重排） |
| 会话记忆 | SQLite，注入最近 8 条（约 4 轮），单条截 1500 字 |
| 评估 | 25 条测试集 / 6 类：通过率 100%，工具准确率 100%，单例均 6.7s |
| 测试 | pytest 21 项全过 |
| 安全 | 每 IP 10 次/分钟；消息 ≤500 字；工具结果截 800 字；Key 只在环境变量 |
| 部署 | GitHub + Render，push 自动部署；免费实例 15 分钟休眠、唤醒约 50s |

---

## 九、面试题库（分层准备，答案要点都在上面章节里）

### A 组：基础必背（30 秒能答，必须滚瓜烂熟）

1. **用一句话介绍这个项目** → 第一章那句话 + "已部署公网可现场演示"
2. **Agent 和普通 ChatGPT 套壳的区别？** → 结论基于真实检测结果 + 排查链路可解释（每步工具卡片可溯源）+ 工具白名单安全管控
3. **Agent 循环是怎么工作的？** → 第四章那张图：messages 传递上下文，LLM 决策、代码执行，tool_call 结果写回循环，max_turns=6 防死循环
4. **用了哪些技术，分别为什么？** → 第三章表格（重点讲"为什么"列）
5. **RAG 是怎么做的？** → 为什么需要（方法论不在模型知识里）→ 切块（按二级标题，语义完整）→ 向量化入库 ChromaDB → 对 Agent 来说是第 5 个工具 → 增强不是依赖
6. **SSE 是什么，为什么不用 WebSocket？** → 单向推送够用、更轻量、浏览器原生支持，匹配"边查边汇报"
7. **安全上做了什么？** → 三道防线：工具全只读 / 函数白名单 / 公网限流+长度限制；Key 只在环境变量
8. **项目怎么部署的？** → GitHub + Render CI/CD，push 自动构建部署，PORT 环境变量适配动态端口

### B 组：深挖追问（考察是不是真做的，答案要有细节）

1. **LLM 返回的 tool_call 参数是流式的，怎么处理？** → 分片到达，tool_acc 字典按 index 拼 id/name/arguments，拼完再执行
2. **怎么防止 LLM 执行危险操作？** → 模型只点名不执行；白名单字典查询，查不到返回"未知工具"；全部只读；subprocess 带 timeout
3. **Embedding 踩过什么坑？** → MiniLM 英文模型中文召回差（"502 排查"命不中 502 文档）→ 混合检索：向量召回 6 条 + 关键词重排（英文数字 token + 中文 2-gram 命中数加权）→ 精准命中；由此理解生产为何用 bge-m3、检索层为何必须可评测
4. **知识库检索挂了会怎样？** → 三级降级（API 向量/本地向量/纯关键词）+ 空库自愈重建；RAG 是增强不是依赖，主流程不中断
5. **多轮记忆怎么实现？LLM 不是没有记忆吗？** → "记忆"= 上下文拼接；SQLite 存问答、下一轮注入最近 8 条；预算管理（单条截 1500 字）防 token 滚雪球；实测三轮后能准确复述第一轮结论
6. **怎么测试 Agent 项目？LLM 输出不确定怎么测？** → 分层：循环逻辑 mock 假 LLM 必须可测（结果写回/白名单/防死循环）；API 层测限流和 SSE 协议；**LLM 效果交给评估体系而不是单测**
7. **怎么评估 Agent 好不好？** → 25 条测试集（6 类故障），每条定义期望工具集合 + 期望结论要点，脚本真实跑批打分：通过率 100%、工具准确率 100%、均 6.7s
8. **评估发现过什么问题？**（最值钱的一题）→ 首轮 96%：内存/cache 问题模型"自己会"跳过了知识库检索 → 收紧 SYSTEM_PROMPT（方法论类必须先检索）→ 复测 25/25。"没有评测集，你永远不知道模型什么时候在凭感觉回答"
9. **线上出问题怎么排查？** → 每请求有 request_id（响应头 X-Request-ID），日志带方法/路径/状态/耗时，用户报 ID 即可定位整条请求；工具调用链路也有日志
10. **ping 工具有什么特殊的？** → 双降级：容器没 ping 命令自动改 TCP 探测（语义不变：可达/不可达）；中文 Windows GBK 编码坑显式处理。体现"工具失效不让服务失效"

### C 组：场景/开放题（考思路，答法是"框架 + 结合项目"）

1. **如果用户量涨 100 倍怎么办？** → LLM 调用是瓶颈：加请求队列 + 结果缓存 + 限流已就位；存储 SQLite 换 PostgreSQL（接口不变，已预留）；实例加配置或上负载均衡
2. **怎么支持企业内部私有文档？** → 演进路线②：文档解析 pipeline（PDF/Word → 文本 → 同样的切块入库流程），可讲切块参数实验
3. **多模态（识别报错截图）会怎么做？** → 需换支持 tool calling 的 vision 模型（如 Qwen-VL），截图 → 视觉模型提取报错文本 → 走现有排查链路。诚实说明：当前模型不支持，这是规划
4. **为什么不用多 Agent？** → 单任务场景用单循环足够，多 Agent 增加协调复杂度；架构上已把能力拆成工具，未来扩展多 Agent 是编排层的事
5. **这个项目最难的点是什么？** → 建议答：流式 tool_call 分片拼装（对协议细节理解要求最高）或 混合检索调优（要真实评测才知道效果）
6. **如果重做一遍你会改什么？** → 参考答案："先建评估集再开发，让每个功能迭代都有数字支撑；其次会考虑 LangGraph 编排，把降级和记忆做成显式状态机"——展示反思能力

### D 组：红线（说错会减分）

- ❌ 不说 Docker / K8s / Prometheus / Kubernetes——部署是 Render，没有容器编排
- ❌ 数字不夸大：测试集是 25 条不是 250 条；知识库 19 条是自己写的语料，不说是"企业级文档"
- ✅ 主动说边界："单目标 Tool Calling 循环，不含任务规划和多 Agent——我的理解是先吃透原理，框架只是封装"
- ✅ 被问不会的："这个我目前只了解思路，还没实践过"比硬编强十倍

---

## 十、项目时间线（讲故事的素材）

| 时间 | 里程碑 |
|---|---|
| 2025.12–2026.05 | 金属企业运维实习：30+ 故障闭环，发现"定位"最耗时 → 项目动机 |
| 2026.05 | 项目启动：手写 Agent 循环 + 4 个检测工具 + SSE 前端，上线 Render |
| 2026.09.18 | RAG 知识库上线（19 条语料 + 混合检索 + 三级降级）；工程化（21→13 项测试起步、request_id 追踪）；踩坑：英文 embedding 中文召回差 → 混合检索 |
| 2026.09.24 | 效果评估体系（25 条测试集，首轮 96% → 修 prompt → 100%，评估闭环）；多轮会话记忆（SQLite + 上下文预算）；线上多轮复验通过 |

**一分钟成长叙事**（面试开场可用）：
> "实习时我发现运维最耗时间的是'定位'，所以做了这个 Agent 把标准排查链路自动化。第一版是纯检测工具循环；上线后我发现模型缺少排查方法论，就加了 RAG 知识库，过程中踩了英文 embedding 中文召回的坑，用混合检索解决；然后我意识到'不知道它好不好'是大问题，就建了 25 条测试集做评估，还真抓出一个 bad case 修掉了；最后补了多轮记忆。每一步都是'发现问题 → 解决 → 验证'的闭环，这正是我做运维养成的习惯。"

---

## 十一、演示指引

现场演示全流程（4 个场景 + 检查清单 + 突发预案）见《现场演示全流程.md》。核心记忆点：
1. 演示前 10 分钟打开链接预热 + **先随便问一个问题触发知识库建库**
2. 场景顺序：RAG（502）→ 全链路排查（qq.com）→ 端口定位 → 多轮记忆追问
3. 手机热点备好；本地 `python main.py` 兜底
