# Ops Agent 现场演示全流程（照着做即可，2026-09-18 全部线上实测）

> 演示地址：https://ai-ops-agent-jmul.onrender.com
> 总时长约 5 分钟：1 分钟开场 + 3 个场景 + 30 秒收尾
> 每个场景标注了【屏幕上会出现什么】，照着指给面试官看就行

---

## 〇、演示前 10 分钟检查清单（不能跳过）

- [ ] **提前 10 分钟打开一次链接**：免费实例 15 分钟无访问会休眠，唤醒约 50 秒（打开后放一边别关）
- [ ] 确认页面加载出**开机自检动画**（2.4 秒，可点击跳过）→ 说明服务活着
- [ ] 手机热点备好：现场 WiFi 可能限制访问外网
- [ ] **本地兜底**：万一线上打不开，cmd 进 `Desktop\111\AI-Ops-Agent\backend` 跑 `venv\Scripts\python main.py`，浏览器开 localhost:8000，效果一样
- [ ] 把演示地址存在手机备忘录，方便当场发给面试官

## 一、开场（1 分钟，开机动画就是开场白）

打开链接，趁开机动画说：

> "这是一个运维控制台风格的设计。它解决的是我在运维实习时最耗时间的环节——**故障定位**。员工说'网站打不开'，新人不知道从哪查起；它让大模型按标准链路自动排查，而且每一步都是**真实执行**的检测，不是模型编的。"

## 二、场景 1：RAG 知识库（主推亮点，放第一个）

**输入**：`Nginx 一直报 502 是什么原因？一般怎么排查？`

**【屏幕上会出现什么】**（已实测）：
- 一张 `search_knowledge` 工具卡片，参数显示 `{"query": "Nginx 502 Bad Gateway 排查思路"}`
- 卡片里能看到检索结果出处：【network.md · HTTP 5xx 错误定位】【systems.md · Nginx 常见故障】
- 大概率会**连续检索两次**（第二次换个角度查），最终给出分层结论

**你说**：
> "注意它自己决定去查知识库——这是 RAG：运维方法论不在模型的知识里，我切块存进向量数据库，Agent 把检索当成一个工具来用。检索结果**带出处、可溯源**，而且知识库挂了不影响排查主流程，这是增强不是依赖。"

**如果面试官追问**：翻《面试话术.md》高频追问表——"Embedding 用的什么？踩过什么坑？"那一条是王牌（英文模型中文召回差 → 混合检索重排）。

## 三、场景 2：真实故障全链路排查（展示"真实检测"）

**输入**：`www.qq.com 网站打不开了，帮我排查`

**【屏幕上会出现什么】**（已实测）：
- 工具卡片按序出现：`dns_lookup` → `ping_host` → `http_check` / `check_port`
- DNS 显示真实解析 IP；HTTP 显示 `200 OK`；端口 443 显示可连接
- 线上容器没有 ping 命令时，卡片会显示"已改用 TCP 探测：443 可连接 → 目标网络可达"（**这本身就是降级设计的展示点**）
- 最后结论：服务端一切正常，问题大概率在**你本地网络/代理**——它会反问用户侧环境

**你说**：
> "每个卡片都是真实执行的结果——DNS 是真解析、端口是真连接。它自己判断了排查顺序，还调了知识库。注意 ping 工具：部署环境是精简容器没有 ping 命令，代码里做了降级，自动改 TCP 探测——**生产环境工具失效不能让服务跟着失效**。"

## 四、场景 3：端口不通定位（展示"能定位到根因"）

**输入**：`帮我看看 github.com 的 3306 端口通不通`

**【屏幕上会出现什么】**：
- `check_port` 卡片显示连接失败
- 结论：端口不通 = 没有服务监听或防火墙拦截，并解释 MySQL 默认 3306、GitHub 不会开放这个端口

**你说**：
> "它能定位到'哪一层断了'：DNS 层、网络层、端口层还是应用层——这正是运维新人最缺的排查链路思维。"

## 五、场景 4：多轮会话记忆（展示 Agent 的"记忆力"）

**先输入**：`帮我查一下 github.com 的 443 端口通不通`
**再追问**：`我刚才让你检测的是哪个网站哪个端口？结果如何？`

**【屏幕上会出现什么】**（已实测）：第二轮**不调用任何检测工具**，直接准确复述"github.com 的 443 端口，端口可连接"。

**你说**：
> "LLM 本身没有记忆，我在服务端做了会话持久化——每轮问答写进 SQLite，下一轮把最近的历史注入上下文，还做了上下文预算管理（历史条数和长度上限），防止多轮对话 token 成本越滚越大。这是 Agent 五要素里的'记忆'。"

## 六、UI 细节展示（30 秒，穿插在场景间）

- **点"Agent 排查过程"按钮条** → 弹出浮窗：每一步的调用参数 + 原始结果 → "全程可追溯、可解释，这是 Agent 和黑盒的区别"
- **右上角切主题**（夜间/日间/护眼）、侧栏统计（真实会话数/工具调用数/耗时）→ "前端是我手写的单文件实现，SSE 流式逐字渲染"

## 七、收尾金句

> "这个项目让我把网络工程的排查经验和大模型应用结合了起来。我认为运维 Agent 落地的关键不是模型多强，而是三件事：**给模型可靠的工具、约束它的执行边界、让排查过程可解释**——这三点我都落地了。"

## 八、突发预案

| 突发 | 应对 |
|---|---|
| 页面 50 秒才响应 | 正常，免费实例唤醒中，顺势说"这是免费云平台冷启动，生产可以加保活" |
| 线上打不开 | 本地兜底（见第〇节）；谈部署："Render CI/CD，push 自动部署" |
| 提问没调知识库 | 换回场景 1 的 502 问法（带"Nginx/502"关键词命中率高） |
| 被问没实现的功能 | 主动说边界："单会话无记忆、非告警驱动，演进路线在规划中"——诚实边界感是加分项 |
| 现场断网 | 手机热点，或讲《面试话术.md》里的架构图 |

---

### 技术参数速记（被追问数字时用）

模型 deepseek-chat ｜ Agent 循环上限 6 轮 ｜ 5 个工具全只读 ｜ 知识库 19 条 ｜ 混合检索 top3 ｜ 限流 10 次/分/IP ｜ 消息 ≤500 字 ｜ pytest 13 项 ｜ 请求追踪 X-Request-ID
